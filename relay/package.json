{
  "name": "askii-relay",
  "version": "0.4.2",
  "private": true,
  "description": "Local mock of the ASKII Cloud Sessions broker (pairing + remote-session command/event routing).",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "dev": "node dist/server.js",
    "test": "npm run build && node --test dist/test/smoke.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "22.x",
    "typescript": "^5.9.3"
  }
}